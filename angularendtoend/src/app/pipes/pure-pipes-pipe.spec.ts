import { PurePipesPipe } from './pure-pipes-pipe';

describe('PurePipesPipe', () => {
  it('create an instance', () => {
    const pipe = new PurePipesPipe();
    expect(pipe).toBeTruthy();
  });
});
