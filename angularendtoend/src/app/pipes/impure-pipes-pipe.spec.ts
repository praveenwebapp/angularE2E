import { ImpurePipesPipe } from './impure-pipes-pipe';

describe('ImpurePipesPipe', () => {
  it('create an instance', () => {
    const pipe = new ImpurePipesPipe();
    expect(pipe).toBeTruthy();
  });
});
